{
  "name": "web-apps",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*/frontend",
    "apps/*/backend",
    "packages/*"
  ],
  "packageManager": "npm@11.1.0",
  "scripts": {
    "prettier": "prettier --write .",
    "test-page": "turbo run dev --filter=test_app-frontend",
    "new-project": "cookiecutter -o apps/ templates/",
    "dev:all": "npm run redis:all && turbo run dev --filter=web_games-* --filter=federation-* --filter=recipe_book-* --parallel",
    "dev:web-games": "npm run redis:federation && turbo run dev --filter=web_games-* --filter=federation-* --parallel",
    "dev:federation": "npm run redis:federation && turbo run dev --filter=federation-* --parallel",
    "dev:recipe-book": "npm run redis:recipe-book && turbo run dev --filter=recipe_book-* --filter=federation-* --parallel",
    "redis:federation": "docker compose -f apps/federation/docker-compose.yml up -d redis 2>/dev/null || true",
    "redis:recipe-book": "docker compose -f apps/recipe_book/docker-compose.yml up -d redis",
    "redis:all": "npm run redis:federation && npm run redis:recipe-book"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^3.3.3",
    "sass": "^1.90.0"
  },
  "dependencies": {
    "@tiptap/core": "^3.10.4",
    "@tiptap/pm": "^3.10.4",
    "@tiptap/starter-kit": "^3.10.4"
  }
}
