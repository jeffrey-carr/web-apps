{
  "name": "web-apps",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*/frontend",
    "apps/*/backend",
    "packages/*"
  ],
  "packageManager": "npm@11.1.0",
  "scripts": {
    "prettier": "prettier --write .",
    "test-page": "turbo run dev --filter=test_app-frontend",
    "new-project": "cookiecutter -o apps/ templates/",
    "dev:web-games": "turbo run dev --filter=web-games-frontend --filter=web-games-backend --filter=test_app-frontend --filter=federation-frontend --filter=federation-backend --no-cache --parallel",
    "dev:federation": "turbo run dev --filter=federation-frontend --filter=federation-backend --no-cache --parallel"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "prettier-plugin-svelte": "^3.3.3",
    "sass": "^1.90.0",
    "sass-embedded": "^1.89.2"
  }
}
