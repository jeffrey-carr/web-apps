name: Deploy App

on:
  push:
    tags:
      - "federation-v*"
      - "recipe_book-v*"
      - "web_games-v*"
      - "fat-bears-v*"

jobs:
  deploy-federation:
    if: startsWith(github.ref_name, 'federation-v')
    uses: ./.github/workflows/deploy-federation.yml
    secrets: inherit
    permissions:
      contents: read
      packages: write

  deploy-recipe-book:
    if: startsWith(github.ref_name, 'recipe_book-v')
    uses: ./.github/workflows/deploy-recipe-book.yml
    secrets: inherit
    permissions:
      contents: read
      packages: write

  deploy-web-games:
    if: startsWith(github.ref_name, 'web_games-v')
    uses: ./.github/workflows/deploy-web-games.yml
    secrets: inherit
    permissions:
      contents: read
      packages: write

  deploy-fat-bears:
    if: startsWith(github.ref_name, 'fat-bears-v') || startsWith(github.ref_name, 'fat_bears-v')
    uses: ./.github/workflows/deploy-fat-bears.yml
    secrets: inherit
    permissions:
      contents: read
      packages: write
