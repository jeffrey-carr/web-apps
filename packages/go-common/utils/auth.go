package utils

import "go-common/types"

// GetUserInfo gets user info from the federation server
func GetUserInfo(env string, userUUID string) (types.CommonUser, error) {
	return types.CommonUser{}, nil
}
